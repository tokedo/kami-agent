# Provider API keys for kami-agent (copy to .env — never committed; .env* is gitignored).
# Only the provider under test is needed for a single-model run; the tri-provider
# smoke tier needs all three.

# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...
